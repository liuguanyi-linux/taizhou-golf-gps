# 2026-09-07 私有测试版验收

## 本次范围

虚拟图 / GPS 工作台独立提取版；保留原图和当前算法，没有重做球洞美术、改变坐标基线或恢复旧自动任务。原项目文件和未提交修改未改动。新仓库为 liuguanyi-linux/taizhou-golf-gps，保持 private，不启用公共网页。

## 已完成检查

- 独立包不连接原业务 API；查看器和传统几何编辑器默认 cn0000385 本地草稿模式。本机地图密钥文件及其页面引用已排除。
- 18 洞数据完整；144 个基础目录点；点位 GeoJSON 为 162 个要素（含 18 个边界），全几何导出为 626 个要素。精确内容以 downloads 清单为准。
- 18 张当前配置 PNG 的原生尺寸全部匹配，18 张参考 JPG 存在。
- GPS、高清图和数据目录的 60 个文件逐字节比对与源文件一致；未重制图片。
- 点位模块 44 项回归通过，配准模块 1000 点往返、序列化、退化输入与外推保护通过。
- 包内 JS 与 HTML 内联脚本语法检查、JSON 解析、已列明的凭据模式与个人路径检查通过。该扫描并非全面渗透测试。
- 实际浏览器打开独立包首页、完整技术文档，查看器加载 18 洞；虚拟图 → 编辑本洞 → 设备预览在同一工作页面可用。此轮只切换显示，不修改用户已有点位或草稿。
- 本地服务器基线下载 HTTP 200；对 .git/config 与本机地图配置路径返回 403。

运行环境：macOS，Node.js 25.8.2，Codex 内置浏览器；预览仅监听 127.0.0.1:3034。3030 未操作。

## 明确未完成

未获得 18 洞现场控制点和独立检查数据；不能保证所有虚拟像素真实经纬度准确。H11 默认配准退化，必须重新校准。独立路线文件只有 H1 候选路线，未现场复核。

未对客户真实球车、专用 GNSS 或多车网络服务器做联调。没有新增云端实时保存、账户登录或朋友专用 HTTPS 在线运行网站。

发行数据是磁盘基线，不含浏览器里个人未导出的点位、校准或线路编辑。仓库源码发布、浏览器编辑导出、现场测量验收是三件不同的事。

## 朋友接入前

先私下分发或授权下载，按 QUICKSTART 操作；测绘校准后导出当前点位校准包，以实际车辆完整 ID 进行 GPS 快照联调。请同时阅读 TECHNICAL 中的双距离类型、两种 GeoJSON、存储和安全限制。
