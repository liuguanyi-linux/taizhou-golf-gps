# 2026-09-17 精度检查更新验收

## 发布顺序

先公开当前工具基线：提交 `a0e2dac`，标签 `v2026.09.17-tool-preview`。GitHub Pages 构建成功，完整发布 ZIP 为 104705786 字节，SHA-256 为 `d188b31b12e1beaf129e2fb9c554bfd784291973bc269ae2bc56464b093b8df1`。随后在最新分支增加本次质量检查，不改写发布标签。

## 自动检查

`node scripts/check.cjs`：原有语法、JSON、基线 18 洞、资源清单、敏感模式扫描和功能回归通过；新增 GNSS 质量检查通过。另执行配准回归：1000 个合成点双向转换在测试容差内，10 m 独立偏移可检出；重复独立点不会重复计数，故意错配 12 m 的控制点触发超过 10 m 的留一偏差。

## 浏览器验证

使用独立的“全流程验收（非实测）”导入项目，未向真实客户数据或原站写入。

| 场景 | 结果 |
| --- | --- |
| 导入过期 RTK 快照 | 明确拒绝；当前洞 JSON 前后完全一致 |
| 导入当前时间的合成固定解快照 | 保存经纬度、仪器、时间、0.04 m / 95% 声明和差分龄期 |
| 地图与高清图测同一对点 | 均显示 268.4 m；标明是直线距离，非沿车道距离 |
| 刷新页面 | 测试点坐标与质量字段保留 |
| 高清图显示过期快照 | 显示“导入快照（非持续定位）”“历史 / 过期位置”和“目标点与底图误差另计” |
| GeoJSON 导出 | 同一坐标与全部设备质量字段保留，配准质量仍为 unverified |
| 手工修改合成设备点经度 | 来源变为 manual_coordinate，accuracy_m 变为 null，固定解等原设备声明清除 |
| 图片配准面板 | 显示图上比例与尚无独立检验；不会将零拟合残差称作实地零误差 |

这里的 0.04 m 是**人为编写的合成设备声明**，不是实际测量结果。268.4 m 是基于两点输入坐标的计算结果，不是卷尺/测距仪真值。

## 未完成的实地验收

没有真实 RTK 接收机、基站改正数、带框架/历元的控制点或现场独立检验数据。本次未验证串口、蓝牙、NTRIP 或持续多车消息流；GPS 权限及真实信号质量需要现场设备验证。后台账号登录后的发布链路仍未验收。视觉生成仍基于源几何，不等于从任意球场名称自动得到精确逐洞测绘。
