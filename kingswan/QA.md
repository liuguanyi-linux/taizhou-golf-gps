# 金沙湾工作台验收记录

日期：2026-09-10。状态：开发预览核心流程验收通过；现场精度未验收。

## 后续复测结果

浏览器操作恢复后，在独立 `?qa=1` 测试区完成以下真实界面操作：

- 18 洞图片逐一切换：全部 complete=true、naturalWidth 大于零，未发现页面脚本错误。
- 未校准虚拟球车只创建一个像素对象，经纬度为空、GPS 候选列表无假点；添加四对人工构造测试控制点后才反算坐标。测试点不属于球场资料。
- 球车拖动坐标从 119.32603897 / 31.74622878 变为 119.32747006 / 31.74667018，刷新后保持；黑 Tee 拖动刷新后保持 119.32429666 / 31.74477258。以上均为合成 QA 数据，不可用于实地。
- 修复路线起点点击已有球车时误切回选择工具的问题；随后三节点折线保存与中间节点拖动通过。三顶点区域保存、增加到四节点、删除回三节点通过。
- 同页设备预览，模拟进度 100% 到道路终点：沿路剩余 0.0 m，终点到测试旗位仍有 105.2 m，未跳到旗位。
- 滚轮缩放后图像放大；复位后图片上下边都在地图视窗内。顶栏、左侧栏不参与图片缩放。未开展全部移动设备尺寸测试。
- 两台球车分别属于第 1 / 18 洞，选择跨洞目标、同时输入两台车位置后距离更新为 146.0 m；未知 ID 拒绝，原有距离保留。
- 导出真实下载文件，验证 18 洞记录、5 个测试要素、4 对测试控制点、field_verified=false；先删除对象再导回该文件成功恢复 5 个对象。浏览器下载事件等待曾超时，但实际文件已产生并经内容验证与导回确认。
- 错误球场 GeoJSON 导入明确拒绝。导入文件选择器曾超时，检查页面状态后正常重试完成。

这些证明核心编辑与数据传递流程能运行，不证明真实球场几何正确。未请求个人设备定位权限，没有保存或上传个人真实定位。以下原记录保留为早期中断历史。

## 已验证

- 独立模块：`workbench/`，18 张视觉草稿与 baseline 随包；不复制第三方攻略原图、用户照片或实时 GPS 记录。
- 浏览器已打开 `http://127.0.0.1:3034/kingswan/`，读取页面 DOM；卫星影像、第 1 洞虚拟图、18 洞按钮、编辑与设备预览入口显示正常。
- 浏览器截图与 DOM 几何检查：页面布局宽度 1280，文档宽度 1280；截图工具返回的可见截图较窄，未将截图截断误判为 CSS 横向溢出。其后缩放交互尚未验收。
- `?qa=1` 显示非实地数据的独立测试工作区。没有给正常工作区写入人为构造的控制点或模拟坐标。
- `node --test scripts/workbench-test.cjs`：14 项算法 / 数据资源测试通过。内容包括数值坐标、配准正反变换、凸包范围、退化控制点、路线顺序 / 拐点 / 终点、GeoJSON 校验、自相交区域、多车 ID、时间与精度校验、18 张图片尺寸 / SHA-256、18 洞 PAR 合计 72 与记分卡差异保留。
- `node --check workbench/workspace.js`：语法检查通过。
- 既有泰州仓库 `node scripts/check.cjs`：44 项原有检查与配准检查、语法/资源检查通过。此项不等于新增金沙湾界面的完整验收。

## 中断与未完成项

浏览器准备选择绘制工具时，自动审批提示额度限制并拒绝操作。未切换其他浏览器或脚本绕过。后续继续的是代码、文档和纯算法测试。

以下必须后续通过真实界面重新验证，不能以单元测试代替：

1. 单击只建一个球车点；无配准时无 GPS；图外点击无副作用。
2. 至少三对控制点、独立检查点、拖动后两图同坐标、刷新持久化。
3. 线 / 面绘制与节点增删、无效形状拒绝且数据不变、撤销。
4. 18 洞逐洞图像加载，缩放后完整复位，设备预览侧栏可见。
5. 导出下载、重新导入、错误球场 / 重复 ID / 无效数据不污染原状态。
6. 多车快照、乱序拒绝、路线模拟终点、真实 GPS 停止与静态位置恢复。

地理层还需核对真实 18 洞顺序，标绘 / 验证 Tee、果岭、障碍和球车道，完成虚拟图配准及现场检查点验收。初始逐洞地理要素仍为空，`field_verified` 不提升。

## 发布门槛

核心界面回归后可按用户要求发布明确标注的“开发预览”。发布基线保留空的逐洞地理要素和配准字段，不包含合成测试点。仍不能以此宣称实地精度通过；公开包继续排除第三方原图和个人来源信息。
